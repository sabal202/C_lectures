#include <iostream>
#include <string>

using namespace std;

int main() {

	string s = "1234567890";
	string::iterator it = s.begin();
	// while (1) {
	// 	cout << *it;
	// 	it++;
	// }
	size_t = pos.find('-');
	command.substr(pos + 1, command.length())

	return 0;
}