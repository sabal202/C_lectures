#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
	// double ex = 1;
	// double x;
	// printf("e^");
	// scanf("%ld", &x);

	// double sum_i = x;
	// int i = 1;
	// double pow_x = 1;
	// double last_ex = -1;
	// while ((ex - last_ex) > 1e-7) {
	// 	printf("ex=%.10ld\tsum_i=%.10ld\ti=%d\n", ex, sum_i, i);
	// 	last_ex = ex;
	// 	++i;
	// 	ex += sum_i;
	// 	sum_i *= x / i;
	// }
	// printf("e^%ld=%.7ld", x, ex);

	// printf("%.7ld\n", 0.0000001);
	// return 0;
}
